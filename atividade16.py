# import math (This imports all features)
# from math import ... (This only imports the requested function)
from math import trunc

num = float(input('Digite um número real: \n'))
print('\nPorção inteira do número: {}.'.format(trunc(num)))